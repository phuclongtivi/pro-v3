import {NextResponse} from "next/server";
import {sql} from "@/lib/event-db";
export async function POST(request:Request){try{const body=await request.json().catch(()=>null);if(!body?.consent||!body?.actionId||!body?.resultCode)return NextResponse.json({ok:false,code:"CONSENT_AND_RESULT_REQUIRED"},{status:400});const db=sql();const id=crypto.randomUUID();await db`insert into long_evolution_metrics(id,generation,action_id,result_code,success,latency_ms,cost_class,anonymous_context,consent_scope) values(${id}::uuid,${String(body.generation||"R4B6")},${String(body.actionId)},${String(body.resultCode)},${Boolean(body.success)},${Number(body.latencyMs||0)},${String(body.costClass||"local")},${JSON.stringify(body.context||{})}::jsonb,${String(body.consent)})`;return NextResponse.json({ok:true,metricId:id})}catch(error){return NextResponse.json({ok:false,code:"METRIC_WRITE_FAILED",message:error instanceof Error?error.message:"Metric error"},{status:500})}}
