"use client";import {useMemo,useState} from "react";import {useSearchParams,useRouter} from "next/navigation";import {FLASH_TEMPLATES} from "@/core/flash-video-templates";export default function Page(){const q=useSearchParams(),r=useRouter();const kind=q.get("kind")||"TVC";const candidates=FLASH_TEMPLATES.filter(t=>kind.includes("Idle")?t.kind==="flash-idle":kind.includes("Training")?t.kind==="ai-training":t.kind==="tvc");const[template,setTemplate]=useState(candidates[0]?.id||"");const[title,setTitle]=useState("");const[text,setText]=useState("");const glyph=useMemo(()=>kind.includes("Training")?"▣◉↝✦⚡✓":"▣◉↝✦♫⚡⌁✓",[kind]);return <main className="page"><section className="hero"><h1>Tạo Video Flash</h1><p>{kind}</p></section><section className="panel form"><div className="step"><span className="badge">1</span><h2>Chọn kịch bản</h2></div><div className="field"><label>Kịch bản nhanh</label><select value={template} onChange={e=>setTemplate(e.target.value)}>{candidates.map(t=><option key={t.id} value={t.id}>{t.name} · {t.duration}s</option>)}</select></div><div className="step"><span className="badge">2</span><h2>Nhập / chọn dữ liệu</h2></div><div className="field"><label>Tên video</label><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Ví dụ: TVC Long TV"/></div><div className="field"><label>Text / yêu cầu</label><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="User chỉ cần mô tả ngắn..."/></div><div className="grid"><div className="field"><label>Hình ảnh</label><input type="file" accept="image/*" multiple/></div><div className="field"><label>Video</label><input type="file" accept="video/*" multiple/></div></div><div className="step"><span className="badge">3</span><h2>AI tư vấn điện ảnh</h2></div><div className="notice">AI Flash sẽ tư vấn composition, camera/lens, lighting, continuity, editing rhythm, color, sound và typography. Provider video là adapter thay thế được.</div><div className="glyphs">{glyph}</div><div className="small">Long Scene Language · LSL-1</div><div className="step"><span className="badge">4</span><h2>Hoàn tất</h2></div><button className="action" onClick={()=>r.push(`/studio?preview=${encodeURIComponent(title||kind)}&kind=${encodeURIComponent(kind)}`)}>Hoàn tất → Phòng Thu</button></section></main>}