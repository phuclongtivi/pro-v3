"use client";import {useState} from "react";import BrandFooter from "@/components/BrandFooter";
const groups=["Sự kiện","AI của Tôi"] as const;const e=["Có Quà Tặng","Không Vé","Có Vé","Sự kiện của tôi"] as const;const a=["AI Flash","AI của tôi","AI Phòng Thu","Quản lý AI và Thu - Chi"] as const;
export default function Page(){const[g,setG]=useState<(typeof groups)[number]>("Sự kiện");const[sub,setSub]=useState<string>("Có Quà Tặng");const list=g==="Sự kiện"?e:a;return <main className="page"><section className="hero"><h1>Long App</h1><p>AI Flash Flow • Event • Music Director</p></section><div className="layerbar">{groups.map(x=><button key={x} className={g===x?"active":""} onClick={()=>{setG(x);setSub(x==="Sự kiện"?"Có Quà Tặng":"AI Flash")}}>{x}</button>)}</div><div className="layerbar">{list.map(x=><button key={x} className={sub===x?"active":""} onClick={()=>setSub(x)}>{x}</button>)}</div><section className="panel"><div className="kpi">{g} → {sub}</div><h2>{sub}</h2><p className="muted">Cây chỉ mục mở theo từng lớp. Chỉ nhánh đang chọn được hiển thị.</p>{sub==="Có Quà Tặng"&&<div className="grid">{["Thông báo 1","Thông báo 2","Thông báo 3"].map(x=><div className="card" key={x}><h3>{x}</h3><p>Check / Watch / Read</p><button className="action">Confirm / Read / Add</button></div>)}</div>}{sub==="Sự kiện của tôi"&&<div className="grid"><div className="card"><h3>Lưu sự kiện</h3><p>Tạo thông báo, lưu và quản lý.</p><button className="action">Mở</button></div><div className="card"><h3>Khởi Tạo Quà Tặng</h3><p>Tạo quà lưu lâu dài vào kho user.</p><button className="action">Mở</button></div></div>}{g==="AI của Tôi"&&<div className="notice">DeepSeek sẽ được gắn server-side qua adapter riêng.</div>}</section><section className="panel" style={{marginTop:16}}><h2>Tuyên bố công nghệ</h2><p className="muted">Long App áp dụng AI Flash Flow™: AI xây dựng mô hình chỉ đạo điểm ảnh, ứng dụng thực thi theo ngân sách điểm ảnh để tối ưu độ phân giải, hiệu suất và nhiệt độ thiết bị.</p></section><BrandFooter/></main>}